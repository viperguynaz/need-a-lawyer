import { SpecialityDirective } from './speciality.directive';

describe('SpecialityDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialityDirective();
    expect(directive).toBeTruthy();
  });
});
